#include <stdio.h>
#include <string.h>

/**
 * main-prints an expression as it is
 * Return: 0 if exited properly and non zero if otherwise
 */

int main(void)
{
	printf("Negassi and Comfort are completing the simple shell project,\n");
	return (0);
}
